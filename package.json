{
  "name": "finflow",
  "version": "1.0.2",
  "//": "Versão sincronizada com src/js/lib/changelog.js — atualize ambos juntos.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint src/ scripts/ vite.config.js",
    "lint:fix": "eslint --fix src/ scripts/ vite.config.js",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "vite": "^5.4.0"
  }
}
